# @package _global_

defaults:
  - _self_

repo_root: ${hydra:runtime.cwd}
data_root: "/home/norbert/datasets/own/clean" # "/mnt/e/own/clean" # "/media/norbert/T7/own/clean"
processed_data_root: "/home/norbert/datasets/own/processed" # "/mnt/e/own/processed" # "/media/norbert/T7/own/processed"

seq_num: "013"
# experiments
output_dir_exp_hg: ${repo_root}/results/exp_hg_own
# gt generation
output_dir_gt_kitti: ${repo_root}/results/gt_own