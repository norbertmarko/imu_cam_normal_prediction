# @package _global_

defaults:
  - _self_
  - ../../paths/paths_kitti

repo_root: ${repo_root}

# seq_num: "039"  # handled in paths_panda.yaml 

output_dir: ${repo_root}/results/eval_kitti

gt_dir: ${repo_root}/results/gt_kitti
exp_dir: ${repo_root}/results/exp_hg_kitti_ts

# reference data directories (expand if you compare with more)
ref_data_dirs:
  - ${repo_root}/results/ref_gnf_kitti

# evaluation parameters
eval_types: ["exp", "paper"]  # options: "exp", "paper"
