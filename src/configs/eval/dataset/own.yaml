# @package _global_

defaults:
  - _self_
  - ../../paths/paths_own

repo_root: ${repo_root}

# seq_num: "039"  # handled in paths_own.yaml 

output_dir: ${repo_root}/results/eval_own

gt_dir: ${repo_root}/results/gt_own
exp_dir: ${repo_root}/results/exp_hg_own_ts

# reference data directories (expand if you compare with more)
ref_data_dirs:
  - ${repo_root}/results/ref_gnf_own

# evaluation parameters
eval_types: ["exp", "paper"]  # options: "exp", "paper"
